<li>"<%= category.title %>" by <b><%= category.tasks  %> <%= <%= task.deadline.strftime('%D') %></b></li>
   <%= link_to 'Destroy', book, method: :delete, remote: true, data: { confirm: 'Are you sure?' } %>